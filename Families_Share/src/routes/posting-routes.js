const express = require('express')
const router = new express.Router()

const Posting = require('../models/family-market/posting')

// Here we add all the routes linked to the postings section

module.exports = router
